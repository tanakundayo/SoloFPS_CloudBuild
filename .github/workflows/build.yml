name: Android APK Build (placeholder)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Show files
      run: ls -la

    - name: Placeholder
      run: echo "🚧 ここに本格的な Unreal APK ビルド処理を入れる予定です"
